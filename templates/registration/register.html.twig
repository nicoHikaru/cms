{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<section class="contenair contentLogReg">
    <div class="box boxLogReg">
        <div class="imgLogReg">
           {# <a href='https://fr.freepik.com/vecteurs/compte-client'>Compte client vecteur créé par vectorjuice - fr.freepik.com</a> #}
           <img src="{{ asset('img/connection.png')}}" alt="connection" width="350px" height="350px"/>
        </div>

        <div id="register">
        <h1>Inscription</h1>

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.email) }}
            {{ form_row(registrationForm.plainPassword, {
                label: 'Mot de passe'
            }) }}
            {{ form_row(registrationForm.agreeTerms) }}

            <button type="submit" class="button mgtForm">Valider</button>
        {{ form_end(registrationForm) }}
        </div>
    </div><!-- box -->
</section>
{% endblock %}
